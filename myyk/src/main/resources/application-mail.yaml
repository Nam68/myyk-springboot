spring:
  mail:
    host: {{host}}
    port: {{port}}
    username: {{mail address}}
    password: {{password}}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true